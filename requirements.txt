# Core scientific computing (used by src/ and evaluate.py)
numpy>=1.24.0
scipy>=1.11.0
matplotlib>=3.7.0

# PDF report generation (generate_pdf.py)
# Install in a separate venv to avoid WeasyPrint system-lib conflicts:
#   python3 -m venv /tmp/pdfvenv && /tmp/pdfvenv/bin/pip install markdown weasyprint
markdown>=3.4.0
weasyprint>=57.0
